<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta content="width=device-width, initial-scale=1, maximum-scale=1, shrink-to-fit=no" name="viewport" />
  <title>Leffet | Admin Panel</title>

  <!-- Existing CSS -->
  <link rel="stylesheet" href="/light/assets/css/app.min.css" />
  <link rel="stylesheet" href="/light/assets/css/style.css" />
  <link rel="stylesheet" href="/light/assets/css/components.css" />

  <link rel="shortcut icon" type="image/x-icon"
    href="https://leffet.in/themes/leo_qos/assets/img/modules/appagebuilder/images/black-logo.png" />

  <style>
    body {
      background: radial-gradient(circle at top left, #e0e7ff, #eef2ff, #dbeafe);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      font-family: "Inter", sans-serif;
    }

    /* ---------- ADMIN TITLE ---------- */
    .admin-title {
      text-align: center;
      font-weight: 800;
      font-size: 1.9rem;
      margin-bottom: 6px;
      color: #111827;
      letter-spacing: 0.5px;
    }

    .admin-subtitle {
      text-align: center;
      font-size: 0.9rem;
      color: #6b7280;
      margin-bottom: 25px;
    }

    /* ---------- CARD ---------- */
    .card-primary {
      border-radius: 18px !important;
      box-shadow: 0 25px 50px rgba(0,0,0,0.15);
      background: rgba(255, 255, 255, 0.9);
      backdrop-filter: blur(12px);
      animation: fadeUp 0.6s ease-out;
      border: none;
    }

    @keyframes fadeUp {
      from { opacity: 0; transform: translateY(25px); }
      to { opacity: 1; transform: translateY(0); }
    }

    .card-body {
      padding: 35px 30px;
    }

    /* ---------- FORM ---------- */
    label {
      font-weight: 600;
      color: #374151;
      margin-bottom: 6px;
    }

    .form-control {
      border-radius: 12px !important;
      padding: 14px !important;
      border: 1px solid #e5e7eb;
      background: #f9fafb;
      transition: all 0.25s ease;
    }

    .form-control:focus {
      background: #fff;
      border-color: #6366f1;
      box-shadow: 0 0 0 4px rgba(99,102,241,0.2);
    }

    /* ---------- ERROR ---------- */
    .error-msg {
      background: rgba(239, 68, 68, 0.12);
      border-left: 4px solid #ef4444;
      padding: 12px;
      color: #991b1b;
      font-weight: 600;
      margin-bottom: 18px;
      border-radius: 8px;
      font-size: 0.9rem;
    }

    /* ---------- BUTTON ---------- */
    .btn-primary {
      background: linear-gradient(90deg, #4f46e5, #6366f1);
      border: none;
      padding: 13px;
      border-radius: 12px !important;
      font-weight: 700;
      font-size: 1rem;
      letter-spacing: 0.4px;
      transition: all 0.3s ease;
    }

    .btn-primary:hover {
      transform: translateY(-3px);
      box-shadow: 0 12px 30px rgba(99,102,241,0.35);
    }

    /* ---------- RESPONSIVE ---------- */
    @media (max-width: 576px) {
      .card-body {
        padding: 28px 22px;
      }

      .admin-title {
        font-size: 1.6rem;
      }
    }
  </style>
</head>

<body>

  <div class="container">
    <div class="row justify-content-center">
      <div class="col-xl-4 col-lg-5 col-md-6 col-sm-9">

        <div class="admin-title">Admin Login</div>
        <div class="admin-subtitle">Secure access to dashboard</div>

        <div class="card card-primary">
          <div class="card-body">

            <% if (msg) { %>
              <div class="error-msg"><%= msg %></div>
            <% } %>

            <form action="/admin/login" method="post">

              <div class="form-group mb-3">
                <label>Email Address</label>
                <input type="email" class="form-control" name="email" required autofocus />
              </div>

              <div class="form-group mb-4">
                <label>Password</label>
                <input type="password" class="form-control" name="password" required />
              </div>

              <button type="submit" class="btn btn-primary btn-lg w-100">
                Sign In
              </button>

            </form>

          </div>
        </div>

      </div>
    </div>
  </div>

  <script src="/light/assets/js/app.min.js"></script>
  <script src="/light/assets/js/scripts.js"></script>
</body>
</html>
